/**
 * Copyright 2023-2024 Shinami Corp.
 * SPDX-License-Identifier: Apache-2.0
 */
export function createRegionalApiUrl(region, chain, service, scheme = "https") {
    return `${scheme}://api.${region}.shinami.com/${chain}/${service}/v1`;
}
export function inferRegionalValueFromAccessKey(accessKey, allValues, defaultValue) {
    var _a;
    const keys = Object.keys(allValues);
    for (const key of keys) {
        if (accessKey.startsWith(key)) {
            return (_a = allValues[key]) !== null && _a !== void 0 ? _a : defaultValue(allValues);
        }
    }
    return defaultValue(allValues);
}
